# Changelog

이 저장소는 `v1.0`부터 버전 태그를 시작합니다 (Git 커밋에 `git tag`로 표시).

## [v2.3] — 2026-09-24 — 예측 검증 도입 + 화면 정직화 (시장 신호 모니터링 대시보드로 재정의)

"이 모델로 실제 사업을 한다면 믿을 수 있는가"를 기준으로 전면 재검증한 결과를 반영.

> ⚠️ **v2.2 이전 문서·발표에 적힌 예측 정확도(단기 MAPE 11.05%, LSTM 9.19%, GBR 4.54% 등)는 무효입니다.**
> 검증 구간의 정답이 학습에 섞인 데이터 누수, 요일 불일치로 한 주가 두 줄로 쪼개진 데이터,
> 파싱 실패 시 화면에 표시되던 하드코딩 기본값에서 나온 수치였습니다.

### 발견 — 정식 워크포워드 백테스트 (`backend/pipelines/honest_backtest.py`)
- 과거 27~39개 시점(예측 기간별)마다 그 시점 데이터로만 재학습, 1~7주 뒤 예측 231회. 날짜 월요일 통일·미래값 채우기 금지·월간 통계 발표지연 6주 반영.
- 앱 방식(가격 수준): 오차 **32.24%** vs 단순 기준선(지난주 값 유지) **15.67%**, 기준선 대비 우세 19.5%, 방향 적중 35.9%, 모델대로 4주 대기 결정 시 구매 단가 **+8.57%**.
- 개선 시도(변화율): 21.59%, 우세 52.8%(p=0.215, 유의하지 않음), 방향 적중 67.5% < '항상 상승' 70.6%.
- 판정 **불합격** → 화면에서 예측 수치를 내림. 검증은 매주 자동 재실행.

### 화면 정직화 — 측정·수집된 값만 표시
- 가격: "현재 계약가 · DDR5 8Gb · $/GB" → **메모리 3사 주가지수(pt)** + "실제 DRAM 계약가가 아닌 대용 지표" 고지.
- 제거: 예측가 카드·예측선·신뢰도(코드에 고정된 81%/74%)·차트 범례의 고정 MAPE, 과거 7주 복기값을 미래로 표시하던 단기 예측, 실제 예측(-60%)과 반대로 보이던 중장기 선.
- 제거한 가짜 요소: 신호 "원본 데이터" 표의 `Math.random()` 값, 사인파로 만든 추이 차트, "재학습 완료·오차 6.0%→3.5% 개선" 고정 문장, 동작하지 않는 HITL 조정판과 원문 버튼, 거시 출처 오기(ICE/S&P/BOK/LME), 존재하지 않는 월(2026-00), "최근 8주 100% 성공", S-002 예측 근거·S-005 Graph RAG·S-013 당시 신호 창, 달력으로 계산한 '마지막 갱신'.
- 인사이트: "CLAUDE 종합 판단 · 신뢰 N%" → "AI 요약 · Gemini · 검증되지 않은 해석". 입력된 사실만 요약하고 예측·신뢰도를 만들지 않음.
- 신호·거시 상세는 실측 26주 이력·실제 출처·기준일로. 신호 설명을 실제 수집 방식대로 수정(B-5/B-6 은 DRAMeXchange/TrendForce 가 아닌 뉴스 감성 점수), B-3/B-7(해커뉴스 점수)을 감성 점수로 잘못 분류하던 것 수정.

### 신선도 감시
- 수집 14일 이상 없음 **또는 같은 값 8주 이상** → "갱신 중단" + 사유. (A-3 관세청은 2026-03 값이 26주째 복사되고 있었음)
- 현재: 14개 신호 중 정상 8 · 갱신 중단 6, 거시 6종 전부 갱신 중단. AI 요약은 갱신 중단 신호를 근거로 쓰지 않음.

### 파이프라인·LLM
- `target-dram`(주가지수) 매주 자동 수집 추가 — 7/12 이후 멈춰 있던 가격 대용 지표 해소.
- LLM 정책: GPT·Gemini·Claude 중 하나, 무료 → **Gemini 무료 티어 단독**(`gemini_client.py`). 중국·오픈소스 모델 미사용. 폐기된 모델(gemini-2.0-flash·1.5-flash-8b·2.5-flash-lite, Groq llama-3.3) 제거, 모델별 무료 한도 순회, 대량 감성분석은 lite 모델 우선.
- 뉴스 제목 한·영 혼합 깨짐 수정(제목 생성 시점부터 고유명사만 치환).
- `forecast_v2.py`: 데이터 누수 수정, macOS OpenMP 충돌로 LSTM 이 멈추던 문제 수정(스레드 1개). 화면 미사용이라 워크플로에서 비차단.
- 워크플로 6단계: 수집 → 뉴스 → **예측 검증** → 예측 학습(참고) → AI 요약 → 화면 데이터.
- A-3/A-4(한국 정부 API)는 GitHub 미국 IP에서 차단되어 한국에서 로컬 수집으로 보충.

### 검증
- 로컬·배포본 모두 41개 화면·창 자동 검사: 금지 표현 0 · 실행 오류 0 · 차트 NaN 0 · 모바일(375px) 가로 넘침 0.
- 한계: 화면 코드(.jsx)는 린트·테스트 대상이 아님(기존 설정). 자동 검사는 금지 표현 목록 기반.

---

## [v2.2] — 2026-07-17 — 수동 갱신 버튼, 아이폰에서 실작동 (무료)

배포된 앱(아이폰 포함)에서 '수동 갱신 실행' 버튼이 로컬 서버 없이 실제로 작동하도록 전면 재구성.

### 구조 (비용 0원)
버튼 클릭 → Vercel 서버리스 함수(`/api/refresh`) → GitHub Actions 워크플로 트리거(공용 주방)
→ 5단계 파이프라인(신호 수집→뉴스→모델 재학습→인사이트→data.js 빌드) → 봇 커밋 → Vercel 자동 재배포
→ 앱이 상태 폴링(`/api/refresh-status`) 후 자동 새로고침 (총 약 5~7분)

### 변경 사항
- **GitHub Actions 워크플로**(`.github/workflows/refresh.yml`): 수동(workflow_dispatch) + 매주 화 06:00 KST 자동. 공개 저장소라 실행 무료.
- **`backend/requirements.txt`**: 파이프라인 의존성 고정(torch CPU 전용) + B-4용 `xlrd` 추가.
- **Vercel 함수 2개**(`api/refresh.mjs`, `api/refresh-status.mjs`): 워크플로 트리거(중복 실행 방지 포함)·상태 조회. 토큰은 Vercel 환경변수 `GH_DISPATCH_TOKEN`에만 저장.
- **RefreshPanel 재작성**: 로컬 uvicorn 의존 제거 → 트리거+폴링+완료 시 자동 새로고침.
- **`vercel.json`**: `/api/*`를 SPA rewrite에서 제외.
- **Secrets 등록 스크립트**(`scripts/set-github-secrets.sh`) + 안내서 2편(`docs/07-guide/`).
- 검증: 실제 버튼 클릭 → 공용 주방 가동 → 봇 커밋 → "✅ 갱신 완료" 표시까지 전 구간 실작동 확인. 신호 수집 10/11 성공(A-4 KOSIS만 해외 IP 차단으로 기존값 유지).

---

## [v2.1] — 2026-07-12 — 모바일 디자인 세부 개선

v2.0(iOS 전용앱 전환) 이후 발견된 모바일 화면의 디테일을 다듬은 패치.

### 변경 사항
- **거시경제 지표 카드**: 값(예: 4.53%)과 긍부정 태그를 한 컬럼에 위·아래로 가운데 정렬. 설명 문구 글자색을 지표명과 동일하게 통일.
- **글로벌 이벤트 카드**: 위험도(고위험 등)와 뉴스유형 태그를 한 컬럼에 위·아래로 가운데 정렬해 제목(설명) 표시 영역 확대.
- **가격 스냅샷(모바일 전용)**: 현재가·1~7주·8~21주 예측가 3장을 카드 1장에 **가로 3칸**으로 통합(칸 사이 구분선), 가격 폰트 26px→15px 축소. 데스크톱은 기존 3카드 레이아웃 그대로 유지(`display:contents` 활용).
- **카드 가로 크기 통일**: 좁은 화면에서 긴 텍스트(이벤트 제목·예측 정확도 기록)가 grid 칸을 화면보다 넓게 밀어내던 버그 수정(`min-width:0`) → 모든 카드 폭이 '예측분석 인사이트' 카드 기준으로 통일됨.
- **섹션 제목 줄 줄바꿈 처리**: '가격 스냅샷'·'DRAM 52주 히스토리'·'글로벌 이벤트 모니터링' 등에서 제목+날짜/설명+버튼이 한 줄에 안 들어가 화면 폭을 넘어가던 문제 수정 — 필요 시 자동 줄바꿈되도록 변경.
- **수집 현황 풋바 줄바꿈 처리**: '이번 주 새 수집 데이터 현황'의 정형/비정형/수집실패/사이클/다음 수집까지 항목이 한 줄에 넘치던 문제 수정.
- **DRAM 차트(모바일)**: 세로 크기 약 30% 확대, 범례를 2컬럼×3줄로 정리(줄 간격 약 50% 축소), 순서를 실측→힌트→Prophet→HistGBR→GBR★→LSTM★로 재배치.
- **단기 모델 비교 표(모바일)**: 왼쪽으로 치우쳐 있던 표를 "모델/MAPE/평가" 균등 3등분 + 가운데 정렬로 수정.
- **거시경제 지표(모바일)**: 지표명·값·설명 모든 컬럼을 가운데 정렬로 통일. 긴 지표명이 2줄로 어색하게 쪼개지던 버그도 함께 수정(줄임말 처리).
- **글로벌 이벤트(모바일)**: 카드형 UI를 거시경제 지표와 동일한 표 형태(구분선 기반 flat row)로 변경. 위험/유형/지역은 가운데 정렬, 모니터링 결과(이벤트 내용)만 왼쪽 정렬 + 최대 2줄 표시. 위험/유형 칸 폭도 축소(82px→62px).
- **수집 현황 카드(모바일)**: 폰트 축소로 4줄 → 2줄로 정리.
- **수동 갱신 실행 설명(모바일)**: 폰트 축소로 2줄 → 1줄로 정리.
- **DRAM 차트 범례 폭 초과 수정(모바일)**: 범례 글자가 카드 폭을 넘어 삐져나오던 문제를 폰트 축소(10px→8px)로 해결.
- **글로벌 이벤트 좌우 여백 축소(모바일)**: 바깥 카드 여백과 각 행 여백이 이중으로 겹쳐 좌우 공간이 남던 문제 수정 — 거시경제 지표처럼 바깥 여백 제거, 오른쪽까지 꽉 채움.

---

## [v2.0] — 2026-07-12 — 메이저 업그레이드

기능을 대폭 축소하고 iOS 전용앱(PWA)으로 전환한 대형 변경이라 메이저 버전(2.0)으로 표기. 사용자 요청 5건 반영.

### 추가/변경
- **iOS 전용앱(PWA) 전환**: 앱 매니페스트(`manifest.webmanifest`)·앱 아이콘(180/192/512)·서비스 워커(`sw.js`, 오프라인 지원) 추가. 아이폰 사파리 '홈 화면에 추가' 시 주소창 없는 전체화면 앱으로 실행.
- **iOS 스타일 디자인**: 좁은 화면(≤640px)에서 하단 탭바(대시보드·뉴스·거시·이벤트·정확도) + 세로 1열 카드 레이아웃 + 노치/홈바 안전여백(safe-area) 적용. 데스크톱은 기존 레이아웃 그대로 유지.
- **뉴스 카드 레이아웃 개선**: 'AI 뉴스 & 감성 분석'에서 제목/출처를 좌우 2컬럼 → 한 컬럼 세로 배치(제목 위, 출처 아래)로 변경.
- **프록시 신호 14→10개**: 예측 영향도 0%로 실측된 A-2(빅테크 CapEx)·B-2(대만 뉴스 감성)·B-3(Reddit/HN)·B-4(지정학 리스크) 제외. 백테스트 결과 단기 MAPE 제거 전/후 동일(16.53%) → 정확도 영향 없음 확인.
- **글로벌 이벤트에서 '기상이변' 카테고리 제외**: 수집 단계·빌드 단계 양쪽에서 필터.
- **Graph RAG(구리 vs DRAM 선행 영향도) 섹션 제거**: 대시보드 카드 + 상세 모달(S-005) + 관련 참조 전부 정리, 섹션 번호 재정렬(04~08).

---

## [v1.1] — 2026-07-12 (`2e54037`)

기존 기능(UI/화면 구성)은 그대로 유지하고, 데이터·모델·인사이트만 최신화한 버전.

### 변경 사항
- **데이터 21개 신호 전체 재수집**: 정형 7(A-1~A-7) + 비정형 7(B-1~B-7) + 거시 6 + 타겟 1(target-dram), 모두 최신 주차까지 갱신
- **뉴스/이벤트 재수집 + 한글화**: RSS 기반 뉴스 10건 + 이벤트 10건, Groq LLM으로 한글 번역 100% 보장
- **Multi-Model 재학습**:
  - 단기(1~7주): Prophet baseline → **XGBoost(우수 모델, MAPE 11.05%)** / LightGBM(17.86%)
  - 중장기(8~21주): **LSTM(PyTorch, held-out MAPE 9.19%)**
- **차트 연결성 버그 수정 (#19)**: 중장기(LSTM) 예측 시작점을 현재가가 아닌 단기(XGBoost) 예측 끝점에 anchor하여, 차트에서 단기→중장기 구간에 생기던 "-53% 절벽" 현상 제거 (갭 0% 확인)
- **`model_comparison.txt` 파싱 견고화**: 텍스트 포맷 변경에도 깨지지 않도록 종료 마커 의존 정규식 → lookahead 방식으로 교체
- **`build_insight.py` LLM fallback 확장**: Anthropic → Gemini → **Groq(신규)** → 휴리스틱 4단계 안전망

### 배포
- Vercel 자동 재배포 (https://sixsense-eta.vercel.app) — 커밋 push 시 자동 트리거

---

## [v1.0] — 2026-06-11 (`7dcd8f9`)

Phase 7까지의 최초 완성 버전 (KAIST CAIO 10기 6조 발표 기준).

### 주요 기능
- 21개 실데이터 신호 자동 수집 파이프라인 (Yahoo/SEC/FRED/KOSIS/관세청/AWS/Manifold/HN/GPR/RSS)
- Multi-Model 앙상블: Prophet + sklearn GBR/HistGBR(단기) + PyTorch LSTM(중장기)
- LLM 종합 인사이트 (Claude → Gemini → 휴리스틱 3단계 fallback)
- 뉴스/이벤트 100% 한글화 (Groq fallback 최초 적용)
- 14개 화면 UI hand-off (Claude Design 원본 1px 불변) + 12회 사용자 명시 확장
- 동적 날짜/학습 cutoff 처리 + anchor 보정 최초 적용
