# Sixsense 작업 규칙 (재발 방지 장치 4 — v2.6, 2026-09-26)

이 파일은 이 프로젝트에서 Claude 가 새 대화를 시작할 때마다 자동으로 읽는다.
배경: 2026-09 두 주 동안 확인된 실수 약 29건(허위 정확도, A-4 엉뚱한 표 4개월, '원천 정체' 오판, 재시도 된다는 거짓 주석,
검사 도구 오판 등)의 반복 원인을 규칙과 자동 장치로 막는다. 상세: `CHANGELOG.md` v2.6.

## 0. 보고
- 모든 보고·진행 문장은 **한글**. 대화 요약(압축) 직후에도 첫 문장부터 한글.
- 상태는 ✅확인됨 / ⚠️미확인 / ❌오류있음 셋 중 하나. 실행해서 확인한 것만 ✅. 오류·경고는 개수까지.
- 판단에는 "왜냐하면 ~" 근거. 의도나 추정을 사실처럼 쓰지 않는다("재시도된다", "원천이 멈췄다" 금지 — 확인 후에만).

## 1. 완료·커밋 전 필수 — `bash scripts/verify.sh`
- 정적 검사 → 자동 시험(정책 검사 포함) → 돌연변이 시험(시험의 시험) → 데이터 의미 검사 → 화면 빌드 → 화면 40개 검사. **하나라도 ❌ 면 커밋·완료 보고 금지.**
- Claude 의 `git commit` 은 `.claude/hooks/verify-before-commit.sh` 훅이 자동으로 verify.sh 를 돌려 실패 시 막는다.
- 화면 40개 검사는 verify.sh 6단계에서 자동(`frontend/scripts/screen-check-run.mjs`, headless 브라우저). 주간 작업에서도 데이터 배포 전에 실행된다.

## 2. 데이터 — "그 지표가 맞는가"
- 신호를 '정상'이라 판정하기 전에 **원천 응답이 정말 그 지표인지**(표 이름·항목·업종·단위·행 수)를 확인한다.
  숫자가 나오고 날짜 형식이 맞는 것은 증거가 아니다. (A-4 는 492만이 넉 달간 '재고지수'로 통과했다)
- '원천 데이터가 멈췄다'고 단정하기 전에 **우리 코드의 조회 기간·조건부터** 확인한다. (A-3 은 코드가 2026-04 로 고정돼 있었다)
- 새 신호·조회 방식을 추가하면 `backend/pipelines/data_checks.py` 의 `RULES` 에 정상 범위·출처 필수 단어를 **먼저** 넣는다.
- `backfill.py`·`upload_manual.py` 같은 '과거 값을 통째로 덮어쓰는' 도구는 만들지 않는다(과거 값 고정과 충돌).

## 3. 고칠 때
- 같은 패턴·조건이 쓰이는 곳을 **전부 검색**한 뒤 고친다(한 곳만 고치고 끝내지 않는다).
- **새 사고가 나면 그 사고를 재현하는 시험을 `backend/tests/test_incidents.py` 에 먼저 추가**하고, 옛 버그를 되살리는 항목을
  `backend/tests/mutation_check.py` 에 추가한다.
- 검사 코드(안전장치·비교 스크립트·금지어 목록)도 코드다 — 정답을 아는 입력으로 먼저 시험한다.
- 통계·가정(독립성, 발표 지연, 반올림 규칙)은 실제 데이터 구조와 대조해 확인한 뒤 쓴다.

## 4. 절차
- 코드 변경은 계획모드로 계획 → 승인 → 단계별 보고(각 단계 끝에 검증 결과) → 사용자 확인 후 다음 단계.
- 버전 태그 전 **독립 검토**(코드 검토 에이전트, 읽기 전용)를 한 번 받고, 지적은 직접 재현해 확인한 뒤 반영.
- 미국 서버(GitHub Actions) 결과와 로컬 결과를 비교해 같아야 할 것이 같은지 확인한다.

## 5. 고정 정책 (사용자 결정)
- LLM 은 Gemini 무료 티어 단독. 중국·오픈소스 모델 금지. API 키·토큰은 Claude 가 입력하지 않는다. `.env` 커밋 금지.
- 예측은 검증 불합격이면 "❌ 불합격 · 참고용"·과거 오차·불합격 이유와 함께만 표시, 방향 화살표 금지.
- A-3·A-4(한국 정부 API)는 미국 IP 에서 간헐 차단 — 실패 시 기존 값 유지, 한국에서 로컬 수집으로 보충.
