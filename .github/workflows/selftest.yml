name: (임시) secret 길이 자가검증
on:
  workflow_dispatch:
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: 더미 secret 길이 출력 (값 노출 안 함)
        env:
          SIXSENSE_SELFTEST: ${{ secrets.SIXSENSE_SELFTEST }}
        run: echo "SIXSENSE_SELFTEST len=${#SIXSENSE_SELFTEST} (기대값=25)"
