# Sixsense 백엔드 Python 의존성 (정확한 버전).
# 사용: pip install -r requirements.txt
#
# Python 3.9 ~ 3.11 권장. macOS Apple Silicon 에서 검증됨.

# ── 데이터 수집 ──
requests==2.32.3
pandas==2.2.3
numpy==1.26.4
yfinance==0.2.50
feedparser==6.0.11
python-dotenv==1.0.1

# ── AI 예측 모델 ──
scikit-learn==1.5.2
prophet==1.1.6
torch==2.5.1                # macOS 는 자동 CPU 빌드, libomp 불필요
# xgboost==2.1.3            # 선택 - macOS 는 brew install libomp 필요
# lightgbm==4.5.0           # 선택 - macOS 는 brew install libomp 필요

# ── 백엔드 API ──
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.3

# ── 데이터 소스 (선택) ──
boto3==1.35.92              # AWS Spot 가격 (A-5)
# google-cloud-bigquery==3.27.0  # GDELT BigQuery (대안)

# ── Supabase 통합 (운영 시) ──
# supabase==2.10.0
